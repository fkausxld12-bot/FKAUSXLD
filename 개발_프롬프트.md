# 꽃 작업장 주문 관리 프로그램 — 개발 프롬프트

> 이 문서를 그대로 AI에게 붙여넣으면 같은 프로그램을 만들 수 있습니다.

---

## 0. 한 줄 요약

경기도 화훼 농장(명정원예영농조합법인 / 하나농장)의 **작업장 컴퓨터**에서 쓰는,
농라F·스마트스토어 주문을 자동으로 긁어와 한 화면에서 보고, 현장 판매 수량을 빼고,
롯데택배 송장까지 자동 등록하는 **로컬 웹 프로그램**을 만들어라.

## 1. 사용자와 상황

- 사용자: 꽃 농장 사장님 한 명. **컴퓨터를 잘 다루지 못한다.**
- 장소: 꽃 작업장 컴퓨터 (Windows). 인터넷은 됨.
- 지금까지 하던 일: 농라F 사이트에서 주문 확인 → 손으로 수량 세기 →
  롯데 파트너 사이트에 송장 하나하나 입력 → 엑셀로 매출 정리.
- 원하는 것: **한 화면에서 다 되게.** 버튼 수가 적고, 한글로 말해 주고,
  잘못 눌러도 되돌릴 수 있어야 한다.

### 절대 조건
1. **설치가 쉬워야 한다** — 압축 풀고 `.bat` 더블클릭이면 끝.
2. **한 화면(One page)** — 탭을 여러 개 만들지 마라.
3. **모든 안내 문구는 한국어**, 기술 용어 금지 ("API 오류" ❌ → "농라F에 로그인하지 못했습니다" ⭕).
4. **업데이트해도 설정이 날아가면 안 된다.**
5. **사용자가 캡처를 보내지 않아도 프로그램이 스스로 문제를 진단해서 한글로 알려줘야 한다.**

## 2. 기술 제약

- **Node.js 22 이상, 순수 표준 라이브러리만.** (`bcryptjs` 하나만 예외 — 네이버 API 서명용)
  프레임워크(Express, React 등) 쓰지 마라. `npm install` 이 실패해도 돌아가야 한다.
- 서버: `http` 모듈로 직접 만든 HTTP 서버, 기본 포트 4000.
- 화면: 정적 HTML + 바닐라 JS. 3초마다 `/api/state` 폴링.
- 브라우저 조종: **Playwright/Puppeteer 금지.** Node 22 내장 `WebSocket` 으로
  **Chrome DevTools Protocol(CDP)** 을 직접 말해라.
- 데이터: `~/.flower-workshop/db.json` (프로그램 폴더 아님 — 업데이트해도 유지되도록).
  저장은 임시파일 쓰고 `rename` 하는 원자적 쓰기.
- 시간대는 항상 `Asia/Seoul`.

### 파일 구성
```
server.js          메인 서버 + 모든 API + 스크래핑 로직
alps.js            롯데택배 송장 자동 입력 (CDP)
selftest.js        자체 점검 (가짜 사이트 띄우고 실제로 돌려봄)
public/index.html  한 화면 UI
public/app.js      화면 로직
public/styles.css  스타일
시작하기.bat        실행 (ASCII 전용, CRLF 줄바꿈)
자체점검.bat        자체 점검 실행
```

> ⚠️ `.bat` 파일에 **한글을 넣지 마라.** Windows CP949에서 깨져서 실행이 안 된다.
> 줄바꿈은 반드시 CRLF.

---

## 3. 기능 1 — 농라F(farmer4989.com) 자동 연동

농라F는 **그누보드5(gnuboard5)** 로 만든 농민 직거래 사이트다. 공식 API가 없으므로
**로그인해서 HTML을 긁어온다.**

### 3-1. 로그인
- `POST {base}/bbs/login_check.php`
  - `mb_id`, `mb_password`, `url=/`
  - `Content-Type: application/x-www-form-urlencoded`
  - **`redirect: 'manual'` 로 보내고, 302 응답이면 로그인 성공**으로 판정.
- 쿠키를 직접 모아서(`set-cookie` 파싱) 이후 요청에 계속 붙여라. 쿠키 항아리 필요.
- 그 외 페이지 요청은 `redirect: 'follow'` (안 그러면 본문이 빈다).

### 3-2. 사이트 경로 자동 감지
사용자가 붙여넣는 주소가 두 종류다.
- 긴 주소: `https://farmer4989.com/gnuboard5/bbs/board.php?bo_table=ymh14141&wr_id=49`
- 짧은 주소: `https://farmer4989.com/ymh14141/49`

짧은 주소만 받으면 `/gnuboard5` 경로를 몰라서 전부 404가 난다.
→ **`/gnuboard5/bbs/board.php`, `/bbs/board.php` 등을 차례로 찔러 보고
살아 있는 경로를 자동으로 찾아서 저장하라.** 사용자에게 묻지 마라.

### 3-3. 판매 상품 · 재고 읽기 (제일 까다로운 부분)

판매글 본문에서 상품·가격·재고를 뽑아야 하는데, **재고가 화면에 안 보이는 곳에 있다.**

1. **숨은 input이 1순위**
   ```html
   <input type="hidden" attr-cont="장미)원탑(sp)" data-idx1="1234" data-idx2="20" ...>
   ```
   - `attr-cont` = 상품명, `data-idx1` = 상품 ID, `data-idx2` = 총 수량
2. **HTML 주석이 2순위** — 재고가 주석 안에 들어 있다.
   ```html
   <!-- 재고 : 16 개 -->
   ```
3. **실시간 재고는 AJAX로 따로 받아라**
   ```
   GET {base}/skin/board/basic/view_gonggoo.cnt.php
       ?total={총수량}&idx={상품ID}&wr_id={글번호}&bo_table={게시판}
   응답 예: 판매_재고_진행   (예: "6_10_0")
   ```
   → `판매`(팔린 수), `재고`(남은 수), `진행`(진행중) 세 숫자.
   화면에 **`8,000원 · 재고:10 진행:0 판매:6`** 처럼 그대로 보여줘라.
   재고가 0이면 **품절**로 표시하고 + 버튼을 막아라.
4. 본문이 `<iframe>` 으로 나뉘어 있으면 iframe 내용까지 받아서 합쳐라.

### 3-4. 주문서 읽기
- 주문 목록 페이지: `https://farmer4989.com/html/mypage_buy_log.php`
  (주소가 바뀔 수 있으니 **메뉴 글자('주문배송', '구매내역')로 링크를 자동 탐색**하라.)
- 한 주문은 이런 덩어리로 되어 있다. **`일시 :` 를 카드 경계로 잡아서 잘라라.**
  (주문번호가 날짜와 다른 줄에 있어서, 날짜 기준으로 자르면 주문번호를 놓친다.)
  ```
  주문완료  입금완료  택배발송  구매확정
  15,000 원
  26-08-02 19:02
  25244427                                  ← 주문번호 (독립된 줄)
  이선희 (yfqfo264308) 010-3326-4308         ← 이름 (아이디) 전화
  [이선희] 인천 미추홀구 소성로163번길 49 (학익동, 타워) 311
  공작초)백공작 (1 개)
  부들레야)보라 (2 개)
  택배요금 : 4000
  ```
- 뽑을 것: 주문번호, 주문일시, 주문자, 전화, 주소, 품목별 수량, 택배요금, 총액.
- **택배요금은 매출에서 빼라.** 매출 = 물품 금액만.
- **이미 등록한 주문번호는 다시 등록하지 마라** (`processed` 집합으로 관리).

### 3-5. 자동 동기화
- 프로그램이 켜져 있으면 **주기적으로(예: 3분) 알아서 확인**한다.
  사용자가 [지금 확인]을 누를 필요가 없어야 한다.
- 새 주문이 들어오면 자동으로 주문 목록·품목별 수량·매출에 반영한다.

---

## 4. 기능 2 — 스마트스토어(네이버) 연동

**커머스API센터** 사용. 사용자는 애플리케이션 ID/시크릿만 넣는다.

```js
// 서명: bcrypt 로 "{clientId}_{timestampMs}" 를 시크릿을 솔트로 해싱 → base64
const pwd  = `${clientId}_${timestamp}`;
const hash = bcrypt.hashSync(pwd, clientSecret);
const sign = Buffer.from(hash).toString('base64');
// POST https://api.commerce.naver.com/external/v1/oauth2/token
//   client_id, timestamp, client_secret_sign, grant_type=client_credentials, type=SELF
```

- 토큰은 만료 전까지 캐시.
- 주문 조회:
  1. `GET /external/v1/pay-order/seller/product-orders/last-changed-statuses?lastChangedFrom=...&lastChangedType=PAYED`
  2. 나온 주문번호들을 `POST /external/v1/pay-order/seller/product-orders/query` 로 상세 조회
- 설정 화면에 **"커머스API센터에 이 컴퓨터의 공인 IP를 등록해야 합니다"** 라고 안내를 띄워라.
  (이거 안 하면 무조건 실패하는데 원인을 알기 어렵다.)

---

## 5. 기능 3 — 현장 판매 (제일 자주 쓰는 화면)

- 농라F에서 긁어온 **상품 목록이 그대로** 여기에 나온다. (따로 상품 등록 만들지 마라)
- 각 줄: `상품명 / 가격 · 재고:N 진행:N 판매:N / [－] 0 [＋]`
- **배송비 없음.** 합계는 물품 금액만.
- [현장 판매 등록] 누르면:
  - 주문 목록에 `현장판매` 로 기록
  - 품목별 수량·매출에 반영
  - **농라F 판매글의 재고를 실제로 깎는다** (아래 7번)
- **실수했을 때 취소할 수 있어야 하고, 취소하면 수량이 다시 +로 복구되어야 한다.**

## 6. 기능 4 — 문자 주문 (현장 판매 안의 버튼)

전화·문자로 들어온 주문을 처리한다. **현장 판매 화면 안의 [📱 문자 주문 등록] 버튼**으로 넣어라.
(별도 화면 만들지 마라 — 사용자가 현장 판매 화면을 제일 편해한다.)

- 입력: 받는 사람 이름 / 전화번호 / 주소 (+ 위에서 고른 수량)
- 처리:
  - **배송비 포함** 금액 계산 — 기본 4,000원, **10만원 이상이면 무료**
  - 수량 차감 (현장 판매와 동일)
  - 주문 목록에 등록
- 출력: **손님에게 보낼 안내 문자 문구를 자동으로 만들어서 복사 버튼과 함께 보여줘라.**
  ```
  [하나농장] 주문 확인되었습니다.
  · 장미)원탑(sp) 2개
  상품 16,000원 + 배송비 4,000원 = 총 20,000원
  입금계좌: <농장 계좌번호>
  입금 확인 후 발송해 드립니다. 감사합니다.
  ```
  계좌번호는 **설정에서 사용자가 넣고, 그 컴퓨터에만 저장**한다.

---

## 7. 기능 5 — 농라F 재고 되돌려 쓰기 (write-back)

현장 판매·문자 주문으로 나간 수량만큼 **농라F 판매글의 재고를 실제로 줄인다.**
사용자가 농라F에 들어가서 손으로 고치지 않아도 되게.

- 수정 페이지: `{base}/bbs/write.php?w=u&bo_table={게시판}&wr_id={글번호}&page=`
- 방법:
  1. 수정 페이지를 로그인 상태로 GET
  2. **폼 안의 모든 input/select/textarea를 통째로 읽어서 그대로 유지**
  3. 해당 상품의 수량 칸(`data-idx1` 로 짝을 맞춘다)만 새 값으로 바꾼다
  4. `write_update.php` 로 전부 다시 POST
- ⚠️ **폼 구조를 추측하지 마라.** 실제 페이지를 읽어서 있는 그대로 되돌려 보내야
  다른 내용(본문, 첨부, 옵션)이 날아가지 않는다.
- 실패하면 조용히 넘어가지 말고 **"농라F 재고를 고치지 못했습니다 — 직접 확인해 주세요"** 라고 알려라.

---

## 8. 기능 6 — 날짜별 매출 (발송글 기준)

이 농장은 **날짜가 자정으로 안 넘어간다.** 판매글(발송글)을 새로 올리는 시각이 하루의 경계다.

> 예: 8/2 18시 발송글이 올라오면, 그 전까지는 8/2 매출.
> 8/3에 새 발송글이 올라오면 거기서부터 8/3 매출.

- 농라F에 **새 판매글(wr_id가 바뀜)이 올라오면 자동으로 다음 날짜로 넘어간다.**
- 그와 별개로 [🆕 새 판매일 시작] 버튼으로 **날짜와 시각을 직접 지정**할 수 있어야 한다.
  (지난 시각으로 소급도 가능 — "어제 18:00부터")
- [되돌리기] 로 방금 나눈 것을 취소할 수 있어야 한다.
- 매출은 **배송비를 뺀 물품 금액.**

## 9. 기능 7 — 송장번호 붙으면 자동 완료

농라F 주문서에 송장번호가 찍히면(예: `롯데택배 1234-5678-9012`),
그 주문을 **자동으로 '완료됨'** 으로 넘겨라. 사용자가 손으로 체크하지 않게.

---

## 10. 기능 8 — 롯데택배 송장 자동 등록 (가장 조심할 부분)

`https://partner.alps.llogis.com` 의 **건별주문접수** 화면에 수하인 정보를 자동으로 채운다.

### 🔒 안전 규칙 — 절대 어기지 마라

1. **사장님이 승인한 건만** 송장을 만든다. 승인 없는 건은 절대 등록하지 않는다.
2. **송하인은 건드리지 않는다.** 화면에 이미 채워진 계약 송하인 값을 그대로 둔다.
   **비어 있으면 채우지 말고 중단하고 사장님께 알린다.**
3. **거래처 · 운임구분(선불) · 집하점도 계약값이다 — 손대지 않는다.**
4. **기본은 저장하지 않는다.** 명시적인 저장 지시가 있을 때만 저장한다.
5. **채소(상추 등)는 자동 판단하지 않는다.** 사장님께 넘긴다.
6. 고객 개인정보(성명·전화·주소)는 **송장 등록에 필요한 동안만** 보관하고,
   완료 14일 뒤 삭제한다. 대화·로그에 주문번호·계좌번호를 남기지 않는다.
7. ALPS는 **쿠키를 쓰지 않고 토큰을 탭 sessionStorage에만** 둔다
   → **로그인은 사장님이 그 창에서 직접** 한다.
   **비밀번호는 절대 대신 입력하지 않는다.** 토큰 유효기간은 로그인 +3시간.
8. 폼은 **교차출처 iframe**(`PIDCUS013U`) 안에 있다.
9. **성공 판정은 화면 메시지가 아니라 재조회로 한다.**
10. **중단 조건**: 승인 회신 없음 / 송하인 비어 있음 / 토큰 만료 /
    주소·수하인·수량 모호 / 채소류 / 이미 등록된 주문(이중 발행 방지)

### 10-1. 브라우저 띄우기 (CDP)

```
chrome.exe --remote-debugging-port=9222
           --user-data-dir=%USERPROFILE%\.flower-workshop\chrome-profile
           --no-first-run --no-default-browser-check
           https://partner.alps.llogis.com/main/pages/sec/authentication
```

- 크롬을 못 찾으면 **엣지·네이버 웨일·Chromium** 순서로 찾아라.
  그래도 없으면 **실행 파일 경로를 물어보고 기억**하라.
- `GET /json/new` 는 요즘 크롬이 막았다. **`Target.createTarget` 을 써라.**
- 이미 떠 있는 창은 `Target.activateTarget` 으로 앞으로 가져오되,
  **`Browser.getWindowForTarget` + `Browser.setWindowBounds({windowState:'normal'})`
  로 OS 창까지 실제로 띄워라.** (탭만 활성화하면 최소화된 창은 그대로 안 보인다)
- **페이지 target이 하나도 없으면 멈춘 창이다** → `Browser.close` 로 죽이고 새로 띄워라.
- 실패하면 `spawn` 오류·종료 코드·stderr를 **한글로 그대로 보여줘라.**
- [강제 다시 열기] 버튼을 따로 둬라.

### 10-2. 폼 칸 찾기 — ID를 믿지 마라

문서에 적힌 ID(`edtAcperNm` 등)와 **실제 사이트의 ID가 다르다.**
→ **화면의 구역 제목과 라벨 글자로 찾아라.**

```
구역: 송하인 / 수하인 / 기타화물정보 / 운임정보 / 집하
라벨: 고객성명 · 전화번호 · 휴대폰 · 주소 · 상세주소 · 주문번호 ·
      주문자명 · 내품수량 · 상품명 · 배달메세지
```

- DOM을 처음부터 훑으면서 **"지금 어느 구역인지"와 "직전 라벨"을 기억**하다가
  input을 만나면 그 짝으로 등록하는 방식.
- **교차출처 iframe** 이므로 `Page.getFrameTree` → 각 프레임마다
  `Page.createIsolatedWorld` → `Runtime.evaluate` 로 들어가라.
  (또는 iframe target에 `Target.attachToTarget {flatten:true}` 로 붙어라)
- `송하인`, `운임정보`, `집하` 구역의 칸은 **쓰기 금지 목록**에 넣고 절대 값을 넣지 마라.

### 10-3. 값 넣기
- 전화번호는 `01x` 로 시작하면 **휴대폰 칸**, 아니면 **전화 칸**.
- 주소는 `동/호/아파트/층` 앞뒤로 잘라서 **주소 / 상세주소**로 나눈다.
- 내품수량은 그 주문의 **총 수량 합계**.
- 넣은 뒤 **어떤 칸에 뭘 넣었는지 사용자에게 보여주고 확인받아라.**
- 저장은 저장 버튼(`fnSave`/`저장`/`btnSave`)을 누른 뒤 2.5초 기다리고,
  **이름 칸을 다시 읽어서 값이 남아 있는지로 성공을 판정**한다.

---

## 11. 화면 구성 (한 페이지, 위에서부터)

```
🌸 꽃 작업장 주문                                    [연결 상태]

[농라 신규 N건] [스토어 신규 N건] [이번 장 주문 N건] [이번 장 매출 N원]

날짜별 매출 (발송글 기준)          [🆕 새 판매일 시작] [되돌리기]
농라F 자동 연동                    [↻ 지금 확인] [설정]
스마트스토어 자동 연동              [↻ 지금 확인] [설정]
현장 판매 (배송비 없음)             [✏️ 농라F 재고 수정 열기] [비우기]
   상품 목록 (재고·판매 실시간)  −  0  ＋
   합계 / [현장 판매 등록] / [📱 문자 주문 등록]
송장 도우미 (롯데택배)   [🖥 송장 창 열기] [상태 확인] [강제 다시 열기] [화면 진단]
품목별 주문 수량 (자동 집계)
주문 목록  [필터] [📄 송장용 엑셀] [＋ 스토어 주문]

                                   [🩺 전체 점검] [전체 비우기]
```

---

## 12. 스스로 진단하기 (아주 중요)

사용자가 컴퓨터를 잘 몰라서 **"안 돼요"** 라는 말밖에 못 한다.
그래서 **프로그램이 스스로 원인을 찾아 한글로 말해야 한다.**

### `GET /api/selfcheck` — [🩺 전체 점검] 버튼
다음을 한 번에 검사해서 `{ lines, problems, ok }` 로 돌려준다.
- 프로그램 버전 / 데이터 저장 위치
- 농라F: 로그인 여부 · 마지막 확인 시각 · 동기화된 상품 수 · 수집한 주문 수 · 오류
- 스마트스토어: 연동 여부 · 오류
- 송장용 브라우저 경로 (못 찾으면 문제로 보고)
- 송장 화면 인식 여부 · 송하인 값 · 인식한 칸 수 · **못 찾은 칸 목록**
- 전체 주문 수 · 이번 장 라벨

**문제마다 "무엇을 어떻게 하면 되는지" 까지 한 문장으로 적어라.**
예: `농라F 상품이 동기화되지 않았습니다. 판매글 주소를 확인하세요.`

### 실패하면 스스로 한 번 더 시도하라
예: 송장 창 열기가 실패하면 자동으로 [강제 다시 열기]로 재시도하고,
시도한 내용을 단계별로 보여줘라.

---

## 13. 자체 테스트 (`node selftest.js`)

**사용자가 설치해서 확인하게 만들지 마라.** 개발자가 먼저 다 돌려보고 줘야 한다.

- **가짜 농라F 사이트**를 로컬 포트에 띄워라 (로그인·판매글·재고 AJAX·주문 목록 전부 흉내).
- **가짜 ALPS**를 두 포트에 띄워라 — 껍데기 페이지와 폼 페이지를 **다른 포트**에 둬서
  **진짜처럼 교차출처 iframe**이 되게 하라.
  폼의 **ID는 `g1`, `h3` 처럼 의미 없게** 만들고 **라벨 글자만 진짜처럼** 둬라.
  (라벨로 찾는 로직이 진짜로 되는지 확인하려면 이렇게 해야 한다)
- 실제 크롬을 headless로 띄워서 **정말로 값이 들어가고 저장되는지** 확인하라.

### 최소한 이 항목들은 통과해야 한다
```
[1] 서버 실행
[2] 농라F: 설정 저장 · 경로 자동 감지 · 로그인 · 상품 동기화 · 실시간 재고 ·
    품절 인식 · 주문 자동 등록 · 배송비 제외 금액 · 주소/연락처 추출
[3] 새 주문 감지 · 판매 수량 실시간 반영 · 송장 붙으면 완료 전환
[4] 현장 판매 등록 · 취소 시 수량 복구
[5] 문자 주문 · 배송비 4,000원 · 10만원 무료 · 안내 문구에 계좌 포함
[6] 발송글 기준 날짜 분리 · 매출은 배송비 제외
[7] 송장: 교차출처 iframe 연결 · 송하인 읽기만 · 라벨로 칸 찾아 입력 ·
    저장 후 재조회 확인 · 송하인 그대로 유지 · 강제 다시 열기로 창 복구
[8] 껐다 켜도 주문·설정 유지 · 중복 등록 없음
[9] 전체 점검이 한국어로 원인을 알려줌
```

결과는 이렇게 보여줘라.
```
✅ 모두 정상입니다. (37개 항목 통과)
```

---

## 14. 그동안 실제로 겪은 함정들 (미리 피해라)

| 증상 | 진짜 원인 | 해결 |
|---|---|---|
| `.bat` 실행하면 알 수 없는 명령 오류 | 한글이 CP949에서 깨짐 | `.bat`은 ASCII만, CRLF |
| 주문이 0건으로 읽힘 | 주문번호가 날짜와 다른 줄에 있음 | `일시 :` 기준으로 카드를 자름 |
| 재고가 0으로 읽힘 | 재고가 HTML 주석과 JS 안에 있음 | 주석 파서 + 숨은 input + AJAX |
| 로그인은 되는데 목록이 404 | 짧은 주소라 `/gnuboard5` 경로를 모름 | 경로 자동 탐지 |
| 페이지 본문이 비어 있음 | `redirect: 'manual'` 로 받음 | 로그인만 manual, 나머지는 follow |
| 고쳤는데 "똑같아요" | 예전 버전이 아직 돌고 있음 | 화면에 **버전 표시** + 새 인스턴스가 옛 인스턴스를 종료 |
| 송장 창이 안 열림 | `GET /json/new` 를 최신 크롬이 막음 | `Target.createTarget` |
| "창을 앞으로 가져왔다"는데 안 보임 | 탭만 활성화하고 OS 창은 그대로 | `Browser.setWindowBounds` 로 창까지 띄우기 |
| 송장 폼을 인식 못 함 | 문서의 ID와 실제 ID가 다름 | 구역+라벨로 찾기 |

---

## 15. 마지막 당부

- **수정 횟수를 줄여라.** 사용자에게 주기 전에 자체 테스트를 다 돌려서 통과시켜라.
- 사용자가 "안 돼요"라고 하면, 캡처를 요구하지 말고
  **[🩺 전체 점검] 결과만 붙여넣으면 원인을 알 수 있게** 만들어라.
- 문구는 전부 한국어. **버튼은 적게, 되돌리기는 항상 가능하게.**
